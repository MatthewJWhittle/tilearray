=== tilearray TileFetcher bench (offline mock) ===
tiles=4  mock_latency=0.03s  server_max_concurrent=2

Scenario A — overload-sensitive mock (server max concurrent = 2, 4 tiles)
Legacy (unbounded)            wall= 0.048s  max_inflight=3  http_attempts=8  retries=4
TileFetcher + EA AIMD preset  wall= 0.064s  max_inflight=2  http_attempts=4  retries=0
TileFetcher + OSM preset      wall= 1.033s  max_inflight=2  http_attempts=4  retries=0

Win: max_inflight capped, HTTP attempts and retry storms reduced (8→4 attempts, 4→0 retries for EA preset). EA preset uses AIMD concurrency (starts at 2, backs off on pressure).

Scenario C — healthy mock (16 tiles, 0.03s latency, no server overload cap)
Legacy (unbounded)            wall= 0.055s  max_inflight=16  http_attempts=16  retries=0
TileFetcher + old EA (1 req/s)  wall=14.034s  max_inflight=2  http_attempts=16  retries=0
TileFetcher + EA AIMD preset  wall= 0.126s  max_inflight=8  http_attempts=16  retries=0
  EA AIMD peak limit=8  concurrency_decreases=0
Win: Skipton-scale (~16 tiles) healthy path — static 1 req/s EA baseline ~14.0s on mock (~25s live) → AIMD ~0.126s on mock (target ~10–13s live; legacy reference 0.055s on mock).

Scenario B — EA fixture offline (ea_lidar_64x64.tif, 4 tiles, 429 + Retry-After)
  bbox reference (OSGB): (431900.0, 382700.0, 431964.0, 382764.0)
TileFetcher + EA AIMD preset  wall= 0.129s  max_inflight=1  http_attempts=8  retries=4
  fixture bytes/tile=16803  successful_retries=4

Re-run: uv run python scripts/bench_fetch_engine.py
