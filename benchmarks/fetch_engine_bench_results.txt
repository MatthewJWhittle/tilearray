=== tilearray TileFetcher bench (offline mock) ===
tiles=4  mock_latency=0.03s  server_max_concurrent=2

Scenario A — overload-sensitive mock (server max concurrent = 2, 4 tiles)
Legacy (unbounded)            wall= 0.047s  max_inflight=3  http_attempts=8  retries=4
TileFetcher + EA preset       wall= 0.063s  max_inflight=2  http_attempts=4  retries=0
TileFetcher + OSM preset      wall= 1.032s  max_inflight=2  http_attempts=4  retries=0

Win: max_inflight capped, HTTP attempts and retry storms reduced (8→4 attempts, 4→0 retries for EA preset). EA preset keeps a modest in-flight cap without a fixed per-second throttle.

Scenario C — healthy mock (16 tiles, 0.03s latency, no server overload cap)
Legacy (unbounded)            wall= 0.052s  max_inflight=16  http_attempts=16  retries=0
TileFetcher + old EA (1 req/s)  wall=14.034s  max_inflight=2  http_attempts=16  retries=0
TileFetcher + EA preset       wall= 0.250s  max_inflight=2  http_attempts=16  retries=0
Win: healthy-path wall time drops when the fixed 1 req/s throttle is removed (14.034s → 0.250s on this mock; legacy reference 0.052s).

Scenario B — EA fixture offline (ea_lidar_64x64.tif, 4 tiles, 429 + Retry-After)
  bbox reference (OSGB): (431900.0, 382700.0, 431964.0, 382764.0)
TileFetcher + EA preset       wall= 0.064s  max_inflight=2  http_attempts=8  retries=4
  fixture bytes/tile=16803  successful_retries=4

Re-run: uv run python scripts/bench_fetch_engine.py
